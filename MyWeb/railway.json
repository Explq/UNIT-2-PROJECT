{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "NIXPACKS"
    },
    "deploy": {
        "startCommand": "cd MyWeb && python3 manage.py migrate && python3 manage.py collectstatic --noinput && gunicorn MyWeb.wsgi"
    }
}